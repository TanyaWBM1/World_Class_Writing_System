# Remaining Edge Cases

## Observed Residual Risks

- Strong motif-return cases can still score only in the warning band when cluster overuse remains high, even if the label is correct.
- Very dense causal progression can coexist with high local cluster density; current scoring handles the benchmarked consequence-chain case, but longer chained arguments may still need further calibration later.
- Cadence-shaped repetition now downgrades from collapse to semantic spin under the guarded relief path, but additional refrain families with lower opening diversity may still sit near the warn/fail boundary.

## Current Status

- No remaining benchmark mismatches in:
  - `redundancy_v2_adversarial_suite`
  - `cadence_stress_suite`

- Residual issues are now boundary risks rather than active benchmark failures.
