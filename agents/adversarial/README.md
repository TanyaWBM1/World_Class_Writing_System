# Adversarial Agents

This layer owns red-team and break-testing responsibilities after meaningful runtime components exist.

It is scaffolded now so the failure surface has an explicit home from the start.
